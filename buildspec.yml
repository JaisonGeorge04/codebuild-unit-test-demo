version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt
  build:
    commands:
      - echo Running Unit Tests...
      - python -m unittest test_app.py
artifacts:
  files:
    - '**/*'
